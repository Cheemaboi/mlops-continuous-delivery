# MLOps Continuous Delivery

A deliberately small Flask inference API used to demonstrate a professional MLOps continuous-delivery pipeline. The project builds a versioned, immutable container artifact once, validates it in staging, and promotes that same artifact to production after GitHub Environment approval.

Implementation, operations, release, and rollback details are added alongside the relevant project milestones.
